// Same comparison core on current harness; old source-specific callers run at the fixed historical source.
import { registerBrowserComparisonCases } from "../bundle9-browser-comparison-cases.mts";
registerBrowserComparisonCases();
